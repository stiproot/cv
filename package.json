{
  "name": "simon-stipcich-portfolio",
  "version": "1.0.0",
  "description": "Simon Stipcich - Software Developer Portfolio",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview --port 5175",
    "generate:pdf": "node scripts/generate-pdf.mjs",
    "build:with-pdf": "npm run docs:build && npm run generate:pdf && npm run docs:build"
  },
  "devDependencies": {
    "vitepress": "^1.0.0",
    "playwright": "^1.48.0"
  },
  "private": true
}
